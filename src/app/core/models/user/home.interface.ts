export interface IBenefit {
    icon: string;
    title: string;
    description: string;
}

export interface IGridItem {
    imageSrc: string;
    title: string;
    description: string;
}

export interface IFAQ {
    question: string;
    answer: string;
    isOpen: boolean;
}